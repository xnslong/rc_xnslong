event_type: "tc375.each_basic"
request:
  body:
    type: mapping
    template:
      products_mapped:
        $source: "@{payload:products}"
        $each:
          product_id: "@{item:id}"
          quantity: "@{item:qty}"
