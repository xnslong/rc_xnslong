event_type: "tc375.each_primitive_with_type"
request:
  body:
    type: mapping
    template:
      items_mapped:
        $source: "@{payload:produce_list}"
        $each:
          product:
            $source: "@{item}"
            $type: string
