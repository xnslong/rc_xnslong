event_type: "tc375.each_with_format"
request:
  body:
    type: mapping
    template:
      orders_mapped:
        $source: "@{payload:orders}"
        $each:
          order_date:
            $source: "@{item:date}"
            $format: "2006-01-02"
          amount: "@{item:total}"
