event_type: "order.paid"
schema:
  type: object
  required: [order_id, user_id, amount, currency]
  properties:
    order_id:
      type: string
    user_id:
      type: string
    amount:
      type: integer
      minimum: 0
    currency:
      type: string
      enum: [CNY, USD, EUR]
