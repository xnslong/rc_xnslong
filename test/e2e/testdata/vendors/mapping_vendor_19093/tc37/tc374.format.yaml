event_type: "tc374.format"
request:
  method: POST
  path: "/api/notify"
  headers:
    Content-Type: "application/json"
  body:
    type: mapping
    template:
      formatted_date:
        $source: "@{payload:paid_at}"
        $format: "2006-01-02"
