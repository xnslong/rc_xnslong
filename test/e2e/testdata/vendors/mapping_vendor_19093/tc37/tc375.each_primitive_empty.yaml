event_type: "tc375.each_primitive_empty"
request:
  method: POST
  path: "/api/notify"
  headers:
    Content-Type: "application/json"
  body:
    type: mapping
    template:
      items_mapped:
        $source: "@{payload:produce_list}"
        $each:
          product: "@{item}"
