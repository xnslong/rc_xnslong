event_type: "tc375.each_basic"
description: "TC3.7-each_basic: basic $each array mapping"
version: 1
schema:
  type: object
  properties:
    products:
      type: array
      description: "商品列表"
      items:
        type: object
        properties:
          id:
            type: string
            description: "商品 ID"
          qty:
            type: integer
            description: "数量"
