event_type: "tc375.each_static"
description: "TC3.7-each_static: $each with static fields mixed in"
version: 1
schema:
  type: object
  properties:
    products:
      type: array
      description: "商品列表"
      items:
        type: object
        properties:
          id:
            type: string
            description: "商品 ID"
