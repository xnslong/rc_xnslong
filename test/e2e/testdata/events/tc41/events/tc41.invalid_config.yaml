event_type: tc41.invalid_config
description: Event for testing config isolation (vendor with invalid YAML coexisting with valid ones)
version: 1
schema:
  type: object
  properties:
    id:
      type: string
