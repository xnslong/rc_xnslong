vendor_id: "ad_platform"
base_url: "https://ad.company.com"
auth:
  type: bearer
  config:
    token: "ad_api_token_xxx"
retry_policy:
  max_attempts: 3
  base_delay: 5s
  max_delay: 60s
  multiplier: 2.0
  jitter: 0.2
response_judgment:
  success:
    type: http_status
