event_type: "order.paid"
request:
  method: POST
  path: "/api/v2/track"
  headers:
    Content-Type: "application/json"
  body:
    type: mapping
    template:
      event: "purchase"
      amount: "@{payload:amount}"
      currency: "@{payload:currency}"
