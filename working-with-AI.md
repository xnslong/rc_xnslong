# AI使用说明

# 与AI的协作

**需求分析**
* 初始阶段
    * 我：将原始需求交给AI，让AI给我整理成用例列表
    * AI: 整理用例列表，覆盖整个业务流程
* 评审阶段
    * 我：评审用例列表，脑暴。识别哪些是真正的需求，哪些是AI错当成了需求。让AI做调研&找更多可能。最终review完成完整的需求。
    * AI: 按照我的要求针对各种场景进行调研。如：AI调研真实的API接口的样子，给出样例，更新用例文档。
* 决定范围阶段
    * 我：划定进MVP的原则，让AI来选择；验证最终MVP范围合理性。
    * AI：确定哪些需求入MVP

**端到端测试用例（黑盒测试）**
* 撰写阶段
    * 我：让AI根据需求文档给出测试用例。
    * AI：梳理了一版本测试用例。
* 评审阶段
    * 我：评审用例，查缺补漏。
    * AI：按照我的要求完善。

**概要设计**
* 撰写阶段
    * 我：让AI基于需求文档生成一份概要设计文档，简述该文档的目的是高层探讨，规定格式要求、详略要求、多方案对比要求等。
    * AI：基于需求分析文档，按照我的要求写了一份基础的概要设计文档
* 评审阶段
    * 我：评审概要文档，对里面的一些架构设计进行重新权衡和取舍，必要时还需要提新的可能性，让AI调研，补充方案可选项。最终为所有的地方确定方案。
    * AI：负责对我的评论展开分析，为我提供更多输入。

**详细设计**
* 撰写阶段
    * 我：让AI基于概要设计，给出详细设计，简述该文档的目的是指导开发，要求格式具体。
    * AI：按照要求撰写初版详细设计文档。
* 评审阶段
    * 我：这个阶段看了看文档，基本没有什么问题了。

**开发**
* 计划阶段
    * 我：让AI先基于需求分析、测试用例文档、概要设计和详细设计，以及一些开发要求，出具一个执行计划。并评审计划中可能存在的问题。
    * AI：编写计划，并按照我的要求修复计划中的问题。
* 开发阶段：
    * AI：负责所有的测试用例、代码的开发，测试环境的搭建（docker拉取PG/MQ等镜像，然后起环境），完成测试，将测试状态添加到用例文档中。
    * 我：评审用例状态的覆盖情况，评审关键代码逻辑的书写。

## 取舍

> 取舍主要发生在概要设计阶段，详见：[概要设计评审记录](/doc/requirements/202605-notification-design-hld.md#附录-a-设计评审记录)。

* 没有采用AI建议的：
    * DB与MQ的职责定位（见：[DB&MQ的定位讨论](/doc/requirements/202605-notification-design-hld.md#a9-mq-与-db-定位关系评审2026-05-23)）
        * 概要设计中引入了DB+MQ，AI建议用DB作为真相源，MQ作为辅助工具。这样当业务发送通知给该通知系统，只要消息入了DB，就算接收成功了，后面就需要保证投递效果。
        * 我拒绝了这个方案，因为这个方案在面对DB/MQ失败的时候，处理逻辑就变得非常复杂。比如DB写入成功，返回给业务说“这条数据我已接收请放心”，但是MQ失败了，那么这条数据实际上就没有办法通过MQ发送，按照AI的方案，就需要在DB中再添加outbox，实现出站表的能力，使系统变得复杂，而且还会加重DB的负担，以后量增长了之后更难以维护。
        * 我给的新方案：对DB和MQ的定位进行了调整：MQ作为系统驱动的核心，消息只有入了MQ成功了，才算成功。DB作为MQ中payload卸载的工具，避免MQ总是携带体积很大的数据在传递，让MQ难以稳定。按照这种思路设计，DB+MQ同时可用，系统才算可用。但是因为DB和MQ都只在做自己擅长的工作，加上我们可以做的一些监控来提前发现风险，快速恢复手段，这样系统的可用性将比AI所推荐的方案好很多。
    * 拼接API请求（见：[拼接方案评审](/doc/requirements/202605-notification-design-hld.md#a3-请求拼装方案评审2026-05-22)）
        * AI推荐使用配置来拼接请求，并给了go-template的方式，可以支持各种表达式，非常灵活。
        * 我拒绝了这个方案：
            * 第一：go template是字符串处理，如果字符串中有引号之类的字符，则可能因为没有转移，而拼出不合法的数据。这对未来配置映射规则的人来说，挑战极大，风险也极高。
            * 第二：go template过于复杂，各种条件表达式。这对配置来说，就非常不简明直观，可能需要非常集中精力的分析，才能知道他想拼什么，拼的对不对。这对未来配置也是灾难。
        * 我的方案：支持配置，但是配置制作一些简单明了的配置，一眼就能看明白。对于高度复杂、高度个性化的情况，代码会更简明直接，以后让相关业务直接写代码，通过插件来支持。
* 应对未来流量增长的方案，也在概要方案中深度讨论了（虽然MVP不需要，但是需要做相关的扩展性余地，让未来这些事情来了之后，能顺利展开）
    * 单体应用拆微服务。当前的服务是接收服务、投递功能、控制能力，整体作为一个单体存在的。未来需要将这3个能力拆成3块，各组部署微服务。服务本身无状态，可以水平扩展。
    * DB：流量增长后，单DB不足时，引入分库设计，让多个DB共同参与。随着时间推移，数据量增加，一定会到一个时间点，单表难以承载了，这个引入按照时间的分表设计。
    * MQ：横向扩展。
    


# 系统边界

我划定MVP版本的定位：可以在真实的环境中工作，服务于正式的业务，要追求服务质量。但是，早期不需要非常严格的流程，也不需要覆盖很多特性（这些可以在后续的演进中逐渐支持）。

### MVP版本的范围选择原则

以下按"做减法"原则梳理各功能模块的 MVP 归属。判断标准：

**不放在 MVP**：
1. 可以逐渐丰富的功能
2. 移除后流程MVP依然可以顺利运行的（如量增长后才能遇到的问题）
3. 仅部分供应商要求的功能

**保留在 MVP**（满足任一即可）：
1. 移除后流程不再完整的
2. 现在不做未来将极度难以扩展/支持的
3. 影响系统效果（成功率、延迟等）的

**MVP决策列表**
* 需求列表: [需求MVP范围](/doc/requirements/202605-notification.md#9-mvp-范围)
* 概要设计：[概要设计MVP范围](/doc/requirements/202605-notification-design-hld.md#631-mvp-功能范围界定)，与需求分析不同，有一些设计，比如分库分表，这些在需求中是没有提及的。


### 当前系统中不解决的

| 问题 | 原因 | 拟解决方式 |
| --- | --- | --- |
| 高度个性化且复杂的 vendor API 请求组装，不在配置方式中解决 |  配置需要简单明了，如果复杂了，配置中藏BUG将更隐蔽更难解，同时配置复杂了也会加重配置的心智负担。 | 以后提供插件集成能力，让vendor运营的人提插件来解决，不在当前系统支持。代码是更适合用来处理这种复杂的个性化映射的。 |
| vendor长期不可用后的投递保障 | vendor短暂失败，提供重试&退避等能力，但是对于长时间存在问题，系统不能解决，因此对这种情况，不尝试做到自动保证投递成功。 | 设置重试次数，重试多次后都失败，则定为死信，并保留一段时间（初定7天）。死信可以人工触发重试（MVP版本暂不支持）。 |

### 可靠性

当前系统提供 **至少一次** 的语义。

> * 当调用vendor API失败后，系统会将消息入延迟队列，一段时间后再重试，直到成功，或者判断为无法到达。
> * 当消息成功投递后，对于同一个消息&同一个vendor接口的投递将不会再重复。

（vendor物理上长期不可用、不可达，这种情况下不保证投递成功）

